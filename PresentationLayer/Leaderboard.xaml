<Window x:Class="PresentationLayer.Leaderboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PresentationLayer"
        mc:Ignorable="d"
        Title="Leaderboard" Height="450" Width="425" MinHeight="450" MinWidth="425" Background="LightGray" WindowState="Maximized">
    <Viewbox>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBox x:Name="GlobalLeaderboard" Height="350" MinWidth="300" VerticalScrollBarVisibility="Visible" />
        <Button  x:Name="CloseWindow" Grid.Row="1" Click="Close_Click" Background="Red" Grid.Column="1" Content="close" Margin="25" Width="100" IsDefault="True"/>
    </Grid>
    </Viewbox>
</Window>
